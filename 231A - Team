n = int(input())
ans = 0
 
for i in range(n):
    count = 0
    arr = list(map(int, input().split()))
    for j in range(3):
        if arr[j] == 1:
            count += 1
    if count >= 2:
        ans += 1
print(ans)
